# Town Center Assets

Place the generated Town Center GLB files in this directory using these names:

- `stage1.glb`
- `stage2.glb`
- `stage3.glb`
- `stage4.glb`

The prototype currently loads `stage1.glb` from the browser at runtime. Do not base64-embed these binary assets into HTML.
